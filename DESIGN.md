# 栏栏菜谱 · 主页设计说明 v3.0

> 更新时间：2026-08-18
> 技术约束：纯静态 HTML / CSS / JavaScript，直接部署到 GitHub Pages，无构建步骤、无第三方运行时依赖。

## 1. 设计目标

新版定位为“温暖的私人食谱手账”，保留原有家庭插画背景与菜谱内容，同时降低模板感：

- 从高饱和渐变、巨型 Emoji 和弹跳动效，改为纸张色、宋体标题、克制线条与轻微位移。
- 让背景插画成为首屏主角，文字移至画面左下，并用渐变保证可读性。
- 菜谱卡以统一的手绘动漫食物插画配合编辑式文字区，用编号、类别、标题、简介和元信息建立层级。
- 将详情弹窗改为桌面右侧抽屉、手机底部抽屉，减少上下文中断。

## 2. 视觉系统

- 背景：继续使用 `assets/hero.jpg`，桌面与手机分别调整图片焦点。
- 主色：暖纸色 `#f3eee6`、墨色 `#292722`、陶土红 `#bd5d3a`。
- 字体：标题优先使用系统宋体，正文使用系统黑体，不加载网络字体。
- 圆角：仅保留轻微圆角；避免大圆角、糖果色和多层阴影造成的组件模板感。
- 菜谱插画：统一为暖色手工纸背景、水彩与彩铅笔触、3:2 横向构图；图片保存在 `assets/recipes/`。
- 动效：使用 CSS 过渡，卡片只做轻微上浮与图片缩放；支持系统“减少动态效果”设置。

## 3. 页面结构

1. 吸顶导航：品牌印章、菜单与关于入口。
2. 首屏：原插画背景、站点标题、简介、菜单入口和手账便签。
3. 菜谱区：搜索、分类、数量反馈与两列图文菜谱卡片。
4. 关于区：说明菜谱站的记录目的。
5. 菜谱详情：准备食材、步骤和小贴士。

## 4. 交互约定

- 搜索范围包含菜名、分类、简介和食材，输入时即时更新。
- 分类包含“全部”、实际存在的菜谱类别和“已收藏”。
- 收藏状态保存在浏览器本地存储中，刷新页面后仍然保留。
- 空结果提供一键清空搜索和筛选的按钮。
- 详情抽屉支持点击遮罩、关闭按钮和 `Esc` 键关闭。
- 打开详情后焦点进入关闭按钮；关闭后回到原菜谱卡片；键盘焦点不会离开抽屉。
- 页面不依赖 `anime.js` 或其他 CDN，网络较慢时也不会影响交互。
- 页面使用内容安全策略限制资源来源，只允许加载本站的脚本、样式与图片。
- 小字号正文与分类标签按 WCAG 2.2 AA 标准保持至少 4.5:1 的颜色对比度。

## 5. 响应式规则

- 大于 680px：菜谱为两列，详情从右侧滑入。
- 小于等于 680px：菜谱为单列，分类可横向滚动，详情从底部滑入。
- 手机端首屏使用独立的背景焦点，保留人物和宠物主体。
- 所有主要触控按钮尺寸接近或大于 38px，详情关闭按钮为 44px。

## 6. 菜谱维护

菜谱数据继续维护在 `script.js` 的 `recipes` 数组中。每条菜谱需要包含：

- `id`：唯一数字编号；
- `image`：菜谱插画的本站相对路径；
- `title`：菜名；
- `category` 与 `categoryLabel`：分类键和中文名称；
- `time`、`difficulty`、`desc`：用时、难度与一句话简介；
- `ingredients`、`steps`：食材与制作步骤数组；
- `tip`：可选小贴士。

新增类别时，卡片仍可正常显示；如需专属主题色，可在 `style.css` 中补充对应的 `data-category` 样式。
