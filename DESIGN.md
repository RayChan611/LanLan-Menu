# 栏栏菜谱 · 主页重构设计文档 v2.0

> 版本：v2.0 ｜ 日期：2026-07-31 ｜ 角色：资深产品经理 + 资深 UI 设计师 → 交付开发
> 参考：下厨房（封面即信息）、豆果美食（卡片信息密度）、Pinterest / 美食 App 编辑式封面
> 技术约束：纯静态 HTML/CSS/JS + GitHub Pages + anime.js（CDN），无构建

---

## 1. 对 v1 的复盘（为什么「一般」）

v1 把卡片从「图标卡」改成了「照片卡」方向，但骨子里还是图标卡，三个硬伤：

1. **emoji 当照片，且信息在图外**：大 emoji 居中 + 下方标题/元信息，读起来像「贴纸」，不像「一道菜的封面」。用户一眼分不清这是菜谱站还是表情包站。
2. **封面是空的**：渐变块上空空荡荡，只有中间一个 emoji，左右上下的空间被浪费，没有「信息密度」。
3. **没有一句话勾子**：卡片没有简介，扫过去不知道这道菜「是什么、为什么想吃」，缺少让人点开的理由。

**结论**：保留 v1 的骨架（导航 + 搜索 + Hero + 分类 + 可爱暖色 + anime.js），**重做卡片为「封面式编辑卡」**，把关键信息「压」到封面上，并补一句简介和难度可视化。

---

## 2. 设计原则（v2）

- **封面即信息**：时间、收藏、分类标签直接落在封面上（仿下厨房把「X人做过」压在照片上）。
- **信息密度 > 留白**：卡片小，但每寸都要有用——封面 3 个信息点 + 标题 + 简介 + 难度。
- **一眼可扫**：不点开也知道「这道菜多难、多久、属于哪类、好吃在哪」。
- **可爱不幼稚**：渐变底 + 圆角 + emoji 保留，但用克制的排版和微交互撑住「成品感」。

---

## 3. 卡片解剖（v2 核心）

```
┌─────────────────────────────┐
│ [⏱ 1.5 小时]        [♡]    │  ← 封面：分类渐变底 + 大 emoji emblem（居中，微投影）
│                            │
│          🍅                │
│                            │
│  [家常菜]                  │  ← 封面底部左：分类胶囊（白透底）
└─────────────────────────────┘
│ 炖番茄牛肋条（腐竹配菜）      │  ← 标题（粗体）
│ 番茄熬出沙，牛肋条软糯入味    │  ← 一句话简介（次要色，单行截断）
│ ●●○ 中等       查看做法 →   │  ← 难度圆点 + CTA
└─────────────────────────────┘
```

### 3.1 封面（`.card-cover`）
- `aspect-ratio: 4/3`，圆角 `18px`，分类渐变底（沿用 v1 五套）。
- 居中 emoji `font-size: 3.6rem` + `drop-shadow`，作为「菜 emblem」。
- 顶部左：**时间胶囊**（白透底 `rgba(255,255,255,.85)` + 文字），含 ⏱。
- 顶部右：**收藏 ♡** 圆按钮（同 v1）。
- 底部左：**分类胶囊**（白透底，显示 家常菜/汤羹…）。
- 封面叠加一层极淡的 bottom-gradient，让底部胶囊更清晰。

### 3.2 信息区（`.card-body`）
- **标题**：`1.15rem / 700`，单行优先，过长省略号。
- **一句话简介** `.card-desc`：新增数据字段 `desc`，`0.85rem / 400`，`--ink-soft`，单行 `text-overflow: ellipsis`（这是 v2 新增的「勾子」）。
- **底部行** `.card-foot`：左 = 难度圆点 `●●○`（简单1/中等2/难3，实心主色、空心浅灰），右 = `查看做法 →`（主色，hover 右移）。

### 3.3 Hover 微交互（anime.js + CSS）
- 卡片：spring 上浮 `-10px` + `scale 1.02` + 阴影增强（同 v1 参数）。
- 封面 emoji：CSS `transform: scale(1.08)` 过渡（0.4s ease），轻微「呼吸」。
- CTA 箭头：CSS `translateX(4px)` 过渡（hover 时右移，提示可点）。
- 收藏点击：anime `scale [1→1.35→1]` spring（同 v1）。

---

## 4. 其他组件微调（保持风格统一）

- **Hero**：保持插画 + 底渐变；标题字重 800 不变。无需大改。
- **分类 chip**：保持动态生成；激活态渐变不变。
- **区块标题**：加一个 🍽️ 小图标前缀，弱化为装饰，不抢戏。
- **滚动条**：沿用 v1 的 5px 克制方案，不动。
- **弹窗**：沿用 v1（头部分类 badge + 标题 + 元信息 + 食材/做法/小贴士），本次不改。

---

## 5. 数据约定变更

`script.js` 的 `recipes` 数组**新增字段 `desc`**（一句话简介，必填）。其余字段不变。
示例：
```
{ ..., desc: "番茄熬出沙，牛肋条软糯入味，腐竹吸饱汤汁" }
```
新增菜必须带 `desc`，否则卡片简介留空会显廉价。

难度圆点映射（开发侧常量，不进数据）：
```
简单 → 1 实心；中等 → 2 实心；难 → 3 实心
```

---

## 6. 动效规范 v2（anime.js 参数）

| 场景 | 目标 | 参数 |
|---|---|---|
| 卡片入场 | `.recipe-card` | `opacity[0→1], translateY[36→0], scale[0.94→1], rotate[-1→0]`, `stagger(80)`, `spring(1,75,14,0)` |
| emoji pop | `.card-emoji` | `scale[0→1.2→1], rotate[-30→10→0]`, `stagger(80,{start:200})`, `spring(1,70,10,0)` |
| 卡片 hover | `.recipe-card` | `translateY:-10, scale:1.02, boxShadow→hover`, `400ms`, `spring(1,80,12,0)` |
| 封面 emoji hover | `.card-emoji` | CSS `scale(1.08)`, `0.4s ease`（非 anime） |
| CTA 箭头 hover | `.card-cta` | CSS `translateX(4px)`, `0.25s ease` |
| 分类点击 | `.cat-btn` | `scale[1→0.92→1]`, `spring(1,80,12,0)` |
| 弹窗开 / 关 | 遮罩/卡/头体 | 沿用 v1 timeline / easeInQuart |
| 收藏点击 | ♡ | `scale[1→1.35→1]`, `spring` |

降级：`anime === undefined` 或 `prefers-reduced-motion: reduce` 时跳过全部动画，内容直接可见。

---

## 7. 响应式

| 断点 | 列数 | 备注 |
|---|---|---|
| ≥1024 | 3–4 列 | 容器 max 1100px |
| 640–1023 | 2 列 | — |
| <640 | 1 列 | 封面更方，简介可两行 |

---

## 8. 开发验收清单

- [ ] 卡片改为「封面式」：时间胶囊 + 收藏 + 分类胶囊 直接落在封面上。
- [ ] 封面底部/信息区有**一句话简介**（来自 `desc` 字段），单行截断。
- [ ] 难度用 **圆点**（●●○）可视化，非纯文字。
- [ ] 标题 + 简介 + 难度 + CTA 信息密度足够，封面不再空荡。
- [ ] hover：卡片上浮 + 封面 emoji 微放大 + CTA 箭头右移，顺滑无冲突。
- [ ] 分类动态生成、搜索实时过滤、收藏切换、弹窗动效 保持 v1 可用。
- [ ] 滚动条克制；三档断点正常；键盘可达 + 减少动效可降级。
- [ ] 提交部署，链接可访问。

---

## 9. 后续（不在本次）

- 真实菜品照片替换 emoji 封面（届时胶囊叠加在照片上更自然）。
- 收藏持久化（localStorage）。
- 「本周招牌」大卡置顶（featured 字段 + 跨列布局）。
